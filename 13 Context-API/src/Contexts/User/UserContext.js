import React from 'react'
// Now within this we will create the context for the user

const UserContext = React.createContext();

//This is the file where we have created the context


export default UserContext
